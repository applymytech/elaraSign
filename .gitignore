# Dependencies
node_modules/

# Build output
dist/

# Build info (generated per deployment)
build-info.json

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Preflight marker (local state)
.preflight-passed

# Deployment state (local tracking)
.preview-revision
.last-live-revision

# Working docs (engineer's local experiments)
devdocs/workingdocs/*
!devdocs/workingdocs/README.md

# Deploy logs (local accountability trail)
devdocs/deploy-logs/*
!devdocs/deploy-logs/README.md

# Test output
coverage/
test-output/

# Temporary files
*.tmp
*.temp

# ============================================================================
# SECRETS - NEVER COMMIT THESE
# ============================================================================

# Deploy config contains personal emails and client patterns - use template
deploy.config.json

# Firestore rules (contains admin email - auto-generated by setup)
firestore.rules

# Firebase config (contains API keys)
web/firebase-config.js

# GCP Service Account Keys (CRITICAL - never commit!)
*.json.key
service-account*.json
credentials*.json
gcp-key*.json

# Actual deployment scripts with real values (use .template versions)
# deploy.ps1 is safe (no secrets), but if you add secrets, ignore it
# setup-gcp.ps1 is safe (no secrets)

# Any file with "secret" in the name
*secret*
*SECRET*

# Private keys
*.pem
*.key

# Certificate directory - folder kept via .gitkeep, secrets ignored
# After git clone, run setup wizard to generate your unique certificate
certs/*.p12
certs/*.password
certs/service.*
!certs/.gitkeep

# ============================================================================
# DEPLOYMENT STATE (local only)
# ============================================================================

# Cloud Build logs
cloudbuild-*.log

# Docker cache
.docker/
